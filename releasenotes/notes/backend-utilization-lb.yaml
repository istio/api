apiVersion: release-notes/v2
kind: feature
area: traffic-management
issue: []
releaseNotes:
  - |
    **Added** `backendUtilization` load balancing policy to `DestinationRule`,
    enabling client-side weighted round-robin based on backend-reported ORCA
    utilization metrics. Endpoints reporting higher utilization automatically
    receive fewer requests.