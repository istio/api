apiVersion: release-notes/v2
kind: feature
area: telemetry
issue:
  - https://github.com/istio/api/pull/2998
releaseNotes:
  - |
    **Added** new configuration options to `OpenTelemetryTracingProvider` in `MeshConfig` to allow configuring Resource Detectors.
