{
  "openapi": "3.0.0",
  "components": {
    "schema": {
      "istio.envoy.config.filter.network.tcp_cluster_rewrite.v2alpha1.TcpClusterRewrite": {
        "description": "TcpClusterRewrite is the config for the TCP cluster rewrite filter.",
        "type": "object",
        "properties": {
          "clusterPattern": {
            "description": "Specifies the regex pattern to be matched in the cluster name.",
            "type": "string",
            "format": "string"
          },
          "clusterReplacement": {
            "description": "Specifies the replacement for the matched cluster pattern.",
            "type": "string",
            "format": "string"
          }
        }
      }
    }
  }
}